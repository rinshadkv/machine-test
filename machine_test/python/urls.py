from django.urls import include, path
from python import views
urlpatterns = [
    # path("home",views.index,),
    # path("add-product",views.add_products,),
    # path("product-billing",views.product_billing,)
]